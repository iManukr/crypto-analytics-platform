<clickhouse>
    <!--
      librdkafka tuning for the Kafka engine tables.
      auto_offset_reset=earliest matters: if ClickHouse starts after Debezium
      has already snapshotted, we still want the snapshot, not just whatever
      arrives next.
    -->
    <kafka>
        <auto_offset_reset>earliest</auto_offset_reset>
        <enable_auto_commit>true</enable_auto_commit>
        <auto_commit_interval_ms>1000</auto_commit_interval_ms>
        <session_timeout_ms>30000</session_timeout_ms>
        <heartbeat_interval_ms>3000</heartbeat_interval_ms>
        <reconnect_backoff_ms>500</reconnect_backoff_ms>
        <reconnect_backoff_max_ms>10000</reconnect_backoff_max_ms>
        <statistics_interval_ms>10000</statistics_interval_ms>
    </kafka>
</clickhouse>
