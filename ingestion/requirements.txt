# Pinned exactly: a data pipeline that silently changes behaviour because a
# transitive dependency moved is worse than one that fails to build.
requests==2.34.2
psycopg2-binary==2.9.12
prometheus-client==0.26.0
